document.querySelectorAll('.operator').forEach(button => {
    button.addEventListener('click', event => {
        // Hier soll die Funktion calculate aufgerufen und das Ergebnis in showResult geschrieben werden.
    });
});

function calculate(operator) {
    // Hier soll die Berechnung durchgeführt werden.
}

function showResult(result) {
    // Hier soll das Ergebnis in das Ergebnisfeld mit der id "result" geschrieben werden.
}
